# 🔧 故障排除指南

## 问题：❌ 无法获取调试信息

这个错误表示 popup 无法与内容脚本通信。以下是排除故障的步骤：

## 🔍 诊断步骤

### 1. 检查连接状态
点击 popup 中的 **🔗 检查连接状态** 按钮：
- ✅ 如果显示"内容脚本连接正常"，则连接没问题
- ❌ 如果显示连接失败，继续下面的步骤

### 2. 常见原因和解决方案

#### 原因 1：页面类型不支持
**错误信息：** `无法在系统页面上运行`
**解决方案：** 确保你在正常的网页上（如小红书），而不是：
- `chrome://` 页面
- `extension://` 页面  
- `about:` 页面
- 新标签页

#### 原因 2：内容脚本未加载
**错误信息：** `Could not establish connection`
**解决方案：**
1. 刷新当前页面（Ctrl+F5 / Cmd+R）
2. 重新加载扩展
3. 点击 **🔄 重新注入脚本** 按钮

#### 原因 3：扩展权限问题
**解决方案：**
1. 检查扩展是否有当前网站的权限
2. 在扩展管理页面启用"允许访问所有网站"
3. 或点击地址栏的扩展图标手动授权

### 3. 手动修复步骤

如果上述方法不起作用，按顺序尝试：

1. **刷新页面**：`Ctrl+F5` (Windows) / `Cmd+R` (Mac)
2. **点击 🔄 重新注入脚本**
3. **等待 3-5 秒**
4. **再次点击 🔗 检查连接状态**
5. **如果连接正常，点击 🛠️ 获取调试信息**

### 4. 验证扩展是否工作

如果连接正常，你应该能看到：
- 🎯 关注按钮数量
- 🚀 Loomi按钮数量  
- 📊 页面总按钮数
- 详细的按钮信息

## 🚀 新增功能

现在 popup 包含以下诊断工具：

- **🔗 检查连接状态**：测试与内容脚本的连接
- **🔄 重新注入脚本**：强制重新加载内容脚本
- **🛠️ 获取调试信息**：显示详细的页面按钮信息
- **🔍 手动扫描Loomi按钮**：强制重新扫描页面

## 📝 仍然无法解决？

如果问题持续存在，请：

1. 打开浏览器开发者工具（F12）
2. 查看控制台是否有错误信息
3. 检查是否看到 "🚀 Loomi Hub Content Script 已加载" 消息
4. 截图错误信息和控制台日志

## 🎯 测试建议

1. 访问 https://www.xiaohongshu.com/
2. 等待页面完全加载
3. 打开扩展 popup
4. 按顺序点击：检查连接状态 → 获取调试信息
5. 查看调试信息面板中的按钮统计

现在你有了完整的诊断工具来解决连接问题！
